# Inspect nuclei with circularity > 1
high_circularity = df_measurements[df_measurements["Circularity"] > 1]
print(f"Nuclei with circularity > 1:\n{high_circularity}")

# Visualize these nuclei
for idx in high_circularity.index:
    coords = measure.regionprops(nuclei_labels)[idx].coords
    plt.figure(figsize=(4,4))
    plt.imshow(nuclei, cmap='gray')
    plt.scatter(coords[:,1], coords[:,0], color='red', s=10)
    plt.title(f"Nucleus {idx} with circularity = {high_circularity.loc[idx, 'Circularity']:.2f}")
    plt.axis('off')
    plt.show()
